[package]
name = "up-wire-xcdrv2"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
up-rust = { path = "../up-rust", default-features = false }

[dev-dependencies]
trybuild = "1.0.114"
